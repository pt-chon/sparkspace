# 当前进度

阶段：v1.1 应用功能与 Windows EXE 启动器已完成。用户已创建目标仓库 https://github.com/pt-chon/sparkspace 并选择 MIT；保留远端初始提交与许可证。

Windows 入口：项目根目录 `灵感屿.exe`；桌面主窗口与悬浮速记快捷方式已更新为 EXE。启动器依次查找构建时 Python、PATH、标准 Python 安装注册表，复用原有启动流程。编译、GUI 可执行文件格式、Python/Tkinter/SQLite、中文空格路径、参数往返和 PATH 回退自检通过；`python test_desktop.py` 回归通过。当前环境的直接 EXE 启动验收被自动审批阻止，需本机双击确认。只读检查确认已有正式服务健康，个人数据不随代码上传。

正式服务：18478，数据目录 `user-data`。桌面入口「灵感屿」「灵感屿 · 悬浮速记」已创建并启动，实际可见窗口分别为 1440 × 960 和 590 × 60；速记栏当前置顶，菜单可关闭置顶。

已完成主题、灵感分支、看板、搜索、回收站、AI 任务包、备份恢复、设备配对与版本冲突保护；原生速记栏支持一行输入、置顶和展开。AI 支持本地 Codex 与 OpenAI 兼容 API，可手动触发或在记录后自动生成建议，默认关闭自动模式。

验收：服务、导出、AI 接口和桌面入口自检通过。v1.1 在真实浏览器中验证聚焦记录、两种多选删除、独立分支保留、回收站恢复、默认模型/提示词继承。单条任务指定 gpt-5.6-luna 与自定义要求，经真实 Codex CLI 完成，目标 1 条、祖先背景 1 条、返回 1 条建议；采纳后父节点正确且原文未改。此处型号是请求配置，不是独立的后端路由证明。

v1.1 范围：主问题分组与聚焦；多选删除、仅删所选保留后代、分支独立；AI 单条/多选范围、模型与提示词设置；4 个新示例主题；UI 改进。升级前已备份数据库，正式库追加示例后为 8 个主题、43 条记录，原 4 个主题/17 条记录逐对象一致。桌面与 390px 手机布局无横向溢出，浏览器控制台无报错。

待实际环境确认：真手机在同一 Wi-Fi 配对；其他 AI 服务的真实 API Key 调用。手机尺寸页面已检查，浏览器验收环境访问局域网 IP 被客户端阻止，不能据此声称真手机联通。公网与云同步尚未部署。
