# 灵感屿 · 灵感工作台

目标：让零碎思考沿着「主题 → 灵感与分支 → 整理 → AI 任务包」形成可用闭环。

## 第一版范围

已确认：独立桌面窗口；多个主题和灵感点；适配世界书、角色卡、量化研究；同一 Wi-Fi 手机访问并同步；保留远程部署方式。

采用：本机 Python 标准库服务与 SQLite 文件；复用已安装 Chrome 的应用窗口；原生 HTML/CSS/JavaScript；无付费 API 和构建依赖。主题信息选填，快速记录优先。卡片与分支视图、搜索、标签、置顶、回收站、JSON 备份恢复、可编辑 Markdown 任务包。

交接内容明确区分想法、问题、决定和资料；默认请 AI 整理，实施必须由用户在导出中选择；选中分支时包含祖先背景。示例主题显著标注为示例。

保存合同：服务端确认后才标记保存成功；SQLite 事务和版本比较防止设备静默覆盖；编辑草稿保留在当前浏览器；恢复前先备份，格式错误不得改变现有数据。手机使用配对码登录；配对码只显示在本机设置页。服务仅供个人可信局域网使用。

追加确认：应用内手动/自动 AI 整理和完善，可调用本地 Codex 或 OpenAI 兼容 API；桌面悬浮输入栏，可选置顶，无须先进入主题。快捷记录默认进入「随手记」，可选其他主题。AI 结果独立保留为建议，采纳整理稿时新增分支保留原文；自动模式由用户在设置中开启。密钥不返回浏览器，CLI 只接收任务文字。

延后：公网部署、账号体系、多人协作、附件索引、任意关联图。这些不影响首版闭环；外出跨网需求出现时再配置 HTTPS 服务或私人 VPN。

## 验收

v1.1 用户反馈升级：主问题分组、折叠与聚焦；多选删除以及保留后代两种语义；分支可提为独立主问题；按单条或多选调用 AI，显式选择后代范围；默认与单次模型和提示词；更多示例主题。分组不改变旧笔记的父子关系，更新只追加新示例，不覆盖已有记录。用户已明确授权上传本应用源码到自己的 GitHub，个人数据库、草稿和凭据排除。

桌面窗口可启动；主题与分支能增改并重启保留；搜索、整理、回收站有效；导出保留约束和内容性质；备份恢复及错误输入通过检查；双会话同步和冲突阻止覆盖；窄屏无主要内容溢出。真手机连接和用户主观体验须由用户实机确认，不用模拟结果替代。

工程依据：single-blueprint；持久蓝图预算 0；用户本次请求已授权本地实施。Library 路由 orchestrate-project-blueprint，快照 2026-08-18，读取 ST-A0 与桌面壳概念资料；没有采纳第三方设计/动画依赖。
