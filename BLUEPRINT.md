# 实施蓝图

1. 数据与服务：固定主题/灵感 schema，实现 SQLite 持久化、版本冲突、配对、备份恢复。证据：隔离数据目录的可运行自检。
2. 主界面：完成主题、快速记录、分支、编辑、搜索、回收站和 AI 任务包。证据：浏览器操作和导出检查。
3. 桌面与手机：一键启动独立窗口，响应式页面，局域网连接设置和同步状态。证据：本机启动、双会话和窄屏验证。
4. 交付：修复验证问题，更新使用说明和 NEXT，交给用户实际体验。

修改边界为本项目目录、应用桌面快捷方式及专用于此服务的私有网络防火墙规则；不改既有项目、不安装全局依赖、不发布公网。AI 自动调用默认关闭，开启后使用用户选择的服务。

首版已完成，当前验证结果及尚待实机确认的范围见 NEXT.md；使用入口见 使用说明.md。
