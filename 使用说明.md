# 灵感屿

给零碎思考一个落点，再把它整理成能交给 AI 的任务。

## 从这里开始

在项目文件夹双击 **`灵感屿.exe`** 即可打开工作台。它是使用本机 Python 与 Chrome 的无控制台启动器，请与整个项目文件夹一起保留；换电脑需要安装 Python（含 Tkinter）和 Chrome。

桌面有两个入口：

- **灵感屿**：完整工作台，管理主题、卡片、分支和 AI 建议。
- **灵感屿 · 悬浮速记**：小输入栏，默认放进「随手记」。输入后按 **Ctrl + Enter** 保存。可以置顶、拖动、展开和切换主题。

悬浮栏的小菜单可以恢复完整面板，设置是否置顶，或打开工作台。普通 Enter 不直接提交，避免影响中文输入。

浏览器入口：电脑访问 <http://127.0.0.1:18478/>。关闭窗口不会关闭同步服务；再次打开会复用已运行的服务。

## 记录与发散

先写下一句话，不用先完善标题或填写背景。整理时打开卡片，可补充标题、正文、标签、来源、内容类型和确认状态。

每个主问题与它的延伸组成一个独立分组。分支在主问题下面展示，可以折叠，或进入「只看这个问题」专注处理。聚焦某个问题后快速记录，新内容会挂在该问题下面。

在卡片上点「继续发散」，新想法会成为它的分支。「灵感卡片」「分支脉络」「整理看板」展示同一份记录；看板按待发散、整理中、已确定排列，在编辑卡片时切换状态。

选中多条记录后可批量删除。删除时可以选择连同后代一起回收，或只删除所选、保留后代；保留的后代会接到最近的未删除上级，没有上级时成为独立主问题。也可以先将某条分支提为独立问题，之后删除原来的主问题就不会连带删除它。删除均进入回收站。

主题可以补充目标、背景、约束和交付要求。内置自由主题、世界书、角色卡和量化研究模板。初始世界书、角色卡、量化主题均为虚构示例，可以修改或移入回收站。

## 让 AI 帮忙

在主题里点 **AI 帮我整理**，选择整理表达、发散完善或推荐归类。也可以从某条记录直接调用，或多选后让 AI 仅处理所选；任务面板会显示处理范围，并可选择是否包含这些记录的后代。必要的上级记录只作为背景，不把其他主问题一起交给 AI 处理。

任务面板可以选择或手输本次模型，修改本次提示词；「数据与设备」里可保存默认模型与默认提示词。本地 Codex 列表来自本机缓存，兼容 API 可以从服务商获取列表；列表可能为空或不是最新，手工填写模型名仍可使用，是否可用取决于账号权限。

任务在后台处理，期间可以继续记录；重新打开 AI 助手可以查看结果和当次范围、模型。

AI 整理稿采用「保存为新灵感／保留为新分支」，不覆盖原始表达；归类建议会移动整条分支，正文保留。AI 生成的内容默认是「整理中」，需要你决定是否定稿。

在 **数据与设备 → AI 助手** 选择调用方式：

- **本地 Codex**：需要本机 Codex CLI 已登录可用。模型留空使用 CLI 默认模型，应用不继承全局 Codex 的工具与插件配置。只提供本次笔记文本，使用只读模式生成建议。此方式调用 Codex 的在线服务，不代表模型在电脑离线推理。
- **OpenAI 兼容 API**：填写服务的 API 地址、模型和 API Key。支持 Chat Completions 接口。Windows 上密钥用当前用户的 DPAPI 加密，不会从配置接口回显；未配置真实密钥前不会调用其他服务。

**自动帮助默认关闭。** 选择「记录后自动整理」或「记录后自动发散完善」后，服务会在修改停止约 25 秒后生成建议。它会使用所选服务的额度，但不会自动采纳。队列限制为一个运行任务和一个等待任务；重启不会偷偷重跑未完成任务。

一次 AI 上下文最多包含 20 个主题摘要、合计 80 个主题与灵感对象、约 12 万字符；发生省略或截断时会提示。内容很多时，按单个主题处理更合适。

也可以选中灵感，点 **导出给 AI**，获得包含背景、约束、原始笔记和交付要求的 Markdown 任务包。只选某条分支时会自动带上上级背景。可编辑预览、复制或下载；预览编辑不改变原始记录。多个主题请分别导出。

## 手机与多设备

1. 电脑运行灵感屿，让手机与电脑连接同一 Wi-Fi／局域网。
2. 电脑点击「连接手机」，在手机浏览器打开显示的局域网地址。
3. 在电脑生成 8 位配对码，输入手机页面。配对码 10 分钟内单次有效。

局域网入口以应用显示的地址为准。当前安装电脑已配置 Windows 私有网络、同子网、指定程序与端口的防火墙规则；换电脑安装需要重新放行。若手机无法连接，请检查电脑是否休眠、Wi-Fi 是否隔离设备，以及当前网络是否被设为私有网络。

已配对的设备访问同一个数据库，页面约每 4.5 秒检查更新。编辑发生冲突时，不会静默覆盖；可以另存一份灵感，或放弃本机草稿载入最新记录。手机尚未提交的草稿仅留在该手机浏览器，清除浏览器数据会清除这些草稿。

跨网络使用可后续通过私人 VPN（例如 Tailscale 或 WireGuard）访问这台电脑，沿用配对机制并按实际网段配置防火墙。当前未配置公网域名、HTTPS 或云同步服务，不建议直接把此个人局域网服务暴露到公网。

## 数据在哪里

- 正式笔记：`user-data/workspace.sqlite3`。
- AI 设置与建议历史：`user-data/ai-state.json`。
- 原生悬浮栏草稿与偏好：`user-data/quick-draft.json`、`quick-window.json`。
- 网页编辑草稿：各设备浏览器本地存储。

「下载完整备份」保存主题、灵感、父子关系和回收站，不包含 API 密钥和 AI 任务历史。恢复会校验格式并保留恢复前快照；SQLite 内保留最近 30 次更新前的版本。需要迁移所有 AI 历史时另行保存整个 `user-data` 目录；DPAPI 密钥不能直接跨 Windows 用户解密，换电脑后重新填写即可。

`data` 目录及 18477 端口属于开发验收实例，正式桌面入口使用 `user-data` 与 18478，不把测试主题当作你的笔记。不要在两份目录间手工混用数据库。

## 开发与维护

本机使用已安装的 Python 3.14、Tkinter、Google Chrome 和 Codex CLI，没有新增运行依赖或付费订阅。

```powershell
python server.py --host 0.0.0.0 --port 18478 --data-dir user-data
python launch.pyw
python launch.pyw --quick
node test_core.mjs
python test_server.py
python test_ai.py
python test_desktop.py --ui
```

运行 `创建桌面入口.ps1` 可重新生成桌面快捷方式。有 `.exe` 时优先使用它；没有时仍可直接用 Python。`build-exe.ps1` 可重新编译启动器。不要把含个人笔记、配对会话或密钥的 `user-data` 提交到代码仓库。

实现参考：[Python SQLite 标准库](https://docs.python.org/3/library/sqlite3.html)、[Chrome 独立窗口显示模式](https://developer.chrome.com/docs/capabilities/display-override)，Codex 参数以本机 `codex exec --help` 核验。
